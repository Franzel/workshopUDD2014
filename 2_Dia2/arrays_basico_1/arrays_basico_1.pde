int[] x = {2,4,6,8,10};

println ("elemento 3 en el array es= " + x[3]);

println("el length(largo) del array es = " + x.length );

for (int i=0; i<x.length ; i++){
  println("elemento "+ i + " es: " + x[i]);
}
